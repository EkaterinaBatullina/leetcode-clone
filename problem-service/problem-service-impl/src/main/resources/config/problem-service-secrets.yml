spring:
  cloud:
    consul:
      host: consul
      port: 8500
  security:
    oauth2:
      resourceserver:
        jwt:
          public-key:

  datasource:
    url: jdbc:postgresql://postgres:5432/problem_service_db
    username: elise
    password: elise

  kafka:
    bootstrap-servers: kafka:9092