# src/test/resources/application-test.yml
spring:
  data:
    mongodb:
      database: testdb
    redis:
      host: localhost
      port: 6379
      timeout: 5000
  cloud:
    consul:
      enabled:
        false

judge0:
  base-url: http://mock-judge0:2358
  callback-url: http://localhost:8080/api/submissions/callback
  batch-size: 5